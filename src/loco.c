#include "api_robot.h"

int main(int args, char **argv)
{
    set_head_servo(0,100);
    return 0;
    // int a = get_us_distance();
    // if(a > 0){
    //     set_torque(30,30);
    // } else {
    //    set_torque(100,100);
    // }
}